import 'package:flutter/material.dart';


class CustomColors {
  //colors must have FF in front when going from hex to Color because Color takes an integer parameter
  static final Color blue = Color(0x3B6C6DBEF5);
  static final Color orange = Color(0xFFF15F22);
  static final Color grey = Color(0xFFededed);
}

